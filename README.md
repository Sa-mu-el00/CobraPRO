
  # Automate Payment Reminder Flow

  This is a code bundle for Automate Payment Reminder Flow. The original project is available at https://www.figma.com/design/z4FR49gv1kTMAB46Kpn2vW/Automate-Payment-Reminder-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  